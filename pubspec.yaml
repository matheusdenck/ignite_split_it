name: split_it
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  equatable:

  cupertino_icons: ^1.0.2
  google_fonts: ^2.1.0

  #facilitador de testes
  mocktail: ^0.1.4
  

  #Firebase
  firebase_core: ^1.13.1
  cloud_firestore: ^3.1.10
  #GoogleLogin
  google_sign_in: ^5.2.4
  #Loading
  shimmer: ^2.0.0
  #date format
  intl: ^0.17.0
  #Gerencia de estado
  mobx: ^2.0.4
  flutter_mobx: ^2.0.2
  mobx_codegen: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.2  

flutter:

  uses-material-design: true

  assets:
    - assets/images/

